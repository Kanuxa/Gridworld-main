# Legacy Gridworld run archive

These run directories were moved intact from the repository root on 2026-08-08. `SHA256SUMS` records pre-move SHA-256 values; the archive tool verified every file after moving. No checkpoints were deleted.
